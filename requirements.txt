requests>=2.31.0
python-dotenv>=1.0.0
streamlit>=1.35.0
pandas>=2.2.0
plotly>=5.20.0
duckdb>=1.0.0
streamlit-sortables>=0.3.1
reportlab>=4.0.0
# Anthropic Claude SDK — powers the AI Assistant page (ai_tools.py +
# the "AI Assistant" page in app.py). Tool-use loop sends questions
# to Claude with access to engine_df / sale_lines via local Python
# tools. Cost: ~$0.50-2/day for typical team usage at this volume.
anthropic>=0.40.0
# v2.67.126 — Fernet symmetric encryption for storing Slack user
# OAuth tokens at rest (Viktor bridge: dashboard posts to Slack
# on the user's behalf so Viktor sees a real human message, not a
# bot). See slack_oauth.py for the encryption helpers.
cryptography>=42.0.0
# v2.67.162 — Postgres driver. Used by migrate_to_pg.py for the
# cross-service drift fix (single shared DB instead of two SQLite
# copies on isolated Render disks). psycopg[binary] ships the
# precompiled libpq so no system-level apt install is needed.
psycopg[binary]>=3.1.0
# v2.67.xxx — chart generation for the Monthly Metrics per-section pie
# charts (dashboard) and monthly_metrics_report.py's PDF. Headless
# (Agg backend), no system binaries needed.
matplotlib>=3.8.0
